#!/usr/bin/env bash
set -euo pipefail

desktop=${XDG_CURRENT_DESKTOP:-}
if [[ -n "${NIRI_SOCKET:-}" || "${desktop,,}" == niri* ]]; then
    exec swaylock -C "$HOME/.config/niri/lock.conf" -f
fi
exec hyprlock

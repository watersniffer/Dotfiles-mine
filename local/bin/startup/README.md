# Startup scripts

Each executable `*.sh` file in this directory is started by `~/.local/bin/at_startup` during both the Hyprland and Niri sessions. The directory is replaced from the repository on each dotfiles install; back up local changes before reinstalling.

Prefix a script with `_` to skip it, or add a `.bak` suffix to disable it temporarily.
